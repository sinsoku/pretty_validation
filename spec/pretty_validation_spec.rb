require 'spec_helper'

describe PrettyValidation do
  it 'has a version number' do
    expect(PrettyValidation::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
